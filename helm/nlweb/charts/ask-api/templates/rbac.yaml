apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "ask-api.fullname" . }}-pod-reader
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "ask-api.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "ask-api.fullname" . }}-pod-reader-binding
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "ask-api.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "ask-api.serviceAccountName" . }}
    namespace: {{ .Values.namespace }}
roleRef:
  kind: Role
  name: {{ include "ask-api.fullname" . }}-pod-reader
  apiGroup: rbac.authorization.k8s.io
