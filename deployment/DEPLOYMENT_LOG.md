# Deployment Log

Zach ran the following:

```
make deploy ENV_NAME=nlweb-westeurope-sweden LOCATION=westeurope COSMOS_LOCATION=swedencentral SQL_LOCATION=swedencentral
```

This deployed everything needed to a new resource group except for the main binary.