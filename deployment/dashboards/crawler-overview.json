{
  "dashboard": {
    "title": "Crawler Overview",
    "tags": ["nlweb", "crawler", "prometheus"],
    "timezone": "utc",
    "refresh": "30s",
    "time": {
      "from": "now-1h",
      "to": "now"
    },
    "panels": [
      {
        "title": "Queue Depth by Status",
        "type": "timeseries",
        "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
        "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
        "targets": [
          {
            "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
            "expr": "crawler_queue_depth{kubernetes_namespace=\"crawler\", kubernetes_pod_name=~\"crawler-master.*\"}",
            "legendFormat": "{{ status }}"
          }
        ]
      },
      {
        "title": "Files Queued Rate",
        "type": "timeseries",
        "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
        "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
        "targets": [
          {
            "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
            "expr": "sum(rate(crawler_files_queued_total{kubernetes_namespace=\"crawler\"}[5m]))",
            "legendFormat": "Files/sec"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "unit": "ops"
          }
        }
      },
      {
        "title": "Scheduler Run Duration P95",
        "type": "timeseries",
        "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
        "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
        "targets": [
          {
            "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
            "expr": "histogram_quantile(0.95, sum(rate(crawler_scheduler_run_duration_seconds_bucket{kubernetes_namespace=\"crawler\"}[5m])) by (le))",
            "legendFormat": "P95"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "unit": "s"
          }
        }
      },
      {
        "title": "Scheduler Runs Total",
        "type": "stat",
        "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
        "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
        "targets": [
          {
            "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
            "expr": "crawler_scheduler_runs_total{kubernetes_namespace=\"crawler\"}",
            "legendFormat": "Total runs"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "unit": "short"
          }
        }
      },
      {
        "title": "Worker Job Throughput",
        "type": "timeseries",
        "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
        "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 },
        "targets": [
          {
            "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
            "expr": "sum(rate(crawler_jobs_processed_total{kubernetes_namespace=\"crawler\"}[5m])) by (job_type, result)",
            "legendFormat": "{{ job_type }} - {{ result }}"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "unit": "ops"
          }
        }
      },
      {
        "title": "Job Duration P95 by Type",
        "type": "timeseries",
        "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
        "gridPos": { "h": 8, "w": 12, "x": 12, "y": 16 },
        "targets": [
          {
            "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
            "expr": "histogram_quantile(0.95, sum(rate(crawler_job_duration_seconds_bucket{kubernetes_namespace=\"crawler\"}[5m])) by (le, job_type))",
            "legendFormat": "{{ job_type }} P95"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "unit": "s"
          }
        }
      },
      {
        "title": "External Call Latency P95 by Service",
        "type": "timeseries",
        "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
        "gridPos": { "h": 8, "w": 12, "x": 0, "y": 24 },
        "targets": [
          {
            "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
            "expr": "histogram_quantile(0.95, sum(rate(crawler_external_call_duration_seconds_bucket{kubernetes_namespace=\"crawler\"}[5m])) by (le, service))",
            "legendFormat": "{{ service }} P95"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "unit": "s"
          }
        }
      },
      {
        "title": "Items Per Job Distribution",
        "type": "histogram",
        "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
        "gridPos": { "h": 8, "w": 12, "x": 12, "y": 24 },
        "targets": [
          {
            "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
            "expr": "sum(rate(crawler_items_per_job_bucket{kubernetes_namespace=\"crawler\"}[5m])) by (le)",
            "legendFormat": "{{ le }}",
            "format": "heatmap"
          }
        ]
      },
      {
        "title": "Worker Status (Idle/Processing)",
        "type": "timeseries",
        "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
        "gridPos": { "h": 8, "w": 24, "x": 0, "y": 32 },
        "targets": [
          {
            "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
            "expr": "crawler_worker_idle{kubernetes_namespace=\"crawler\"}",
            "legendFormat": "{{ pod }}"
          }
        ],
        "fieldConfig": {
          "defaults": {
            "mappings": [
              { "type": "value", "options": { "0": { "text": "Processing" } } },
              { "type": "value", "options": { "1": { "text": "Idle" } } }
            ]
          }
        }
      }
    ],
    "templating": {
      "list": [
        {
          "name": "DS_PROMETHEUS",
          "type": "datasource",
          "query": "prometheus",
          "current": {
            "text": "Managed_Prometheus_mon-nmmchkjjxoonu",
            "value": "mon-nmmchkjjxoonu"
          }
        }
      ]
    },
    "schemaVersion": 39
  }
}
